# Allow image files to be served
<FilesMatch "\.(png|jpg|jpeg|gif|webp)$">
    Allow from all
</FilesMatch>

# Block PHP execution
<FilesMatch "\.php$">
    Deny from all
</FilesMatch>

# Disable directory listing
Options -Indexes
